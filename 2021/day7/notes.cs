// for (int i=1; i<=Math.Abs(steps); i++) {
//         fuelCost += i; 
//     }

// foreach (int pos in crabLocations) {

//   int fuelCost = 0;

//   foreach (int crabPos in crabLocations) {

//     int steps = crabPos - pos;

  // var crabDifferences = crabLocations.Select(x => Math.Abs(x - position));
                // foreach (int step in crabDifferences)
                // {  } 